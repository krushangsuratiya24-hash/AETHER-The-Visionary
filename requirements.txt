# AETHER — THE VISIONARY: Core Dependencies
# Python: 3.12+ (Tested & verified on Python 3.13.7 64-bit on Windows 11)

# Phase 0, 1 & 2 dependencies
opencv-python>=4.10.0
mediapipe>=1.0.1
numpy>=2.0.0
pygame-ce>=2.5.0
pytest>=8.0.0

# Phase 3 — Phase Shift
# Real microphone audio capture (clap detection)
# sounddevice uses PortAudio; on Windows this is bundled with the wheel.
sounddevice>=0.4.6
